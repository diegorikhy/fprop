// Generated by CoffeeScript 1.9.3
(function() {
  angular.module("fullApp", ["ngTouch"]).controller("Full", [
    "$scope", "$rootScope", "$window", "$timeout", function($scope, $rootScope, $window, $timeout) {
      var isMobile, limpaActiveMenu, w;
      w = angular.element($window);
      $scope.vars = {
        flatStyle: true,
        isOnline: false,
        contrate: false,
        showLogin: false
      };
      $scope.site = {
        menu: [
          {
            nome: "Cases",
            link: "#cases",
            active: true
          }, {
            nome: "Somos a Full",
            link: "#somos-full",
            active: false
          }, {
            nome: "Clientes",
            link: "#clientes",
            active: false
          }, {
            nome: "Fullanos",
            link: "#fullanos",
            active: false
          }, {
            nome: "Contatos",
            link: "#contato",
            active: false
          }
        ]
      };
      $scope.activeMenu = function() {
        return $timeout($scope.slideRight, 300);
      };
      limpaActiveMenu = function() {
        var i, item, len, ref, results;
        ref = $scope.site.menu;
        results = [];
        for (i = 0, len = ref.length; i < len; i++) {
          item = ref[i];
          results.push(item.active = false);
        }
        return results;
      };
      w.scroll(function() {
        var mnCases, mnClientes, mnContato, mnFullans, mnSomosFull, scrollTop;
        mnCases = angular.element("#cases").offset().top;
        mnSomosFull = angular.element("#somos-full").offset().top;
        mnClientes = angular.element("#clientes").offset().top;
        mnFullans = angular.element("#fullanos").offset().top;
        mnContato = angular.element("#contato").offset().top;
        limpaActiveMenu();
        scrollTop = w.scrollTop() + 100;
        if ((mnCases < scrollTop && scrollTop < mnSomosFull)) {
          $scope.site.menu[0].active = true;
        } else if ((mnSomosFull < scrollTop && scrollTop < mnClientes)) {
          $scope.site.menu[1].active = true;
        } else if ((mnClientes < scrollTop && scrollTop < mnFullans)) {
          $scope.site.menu[2].active = true;
        } else if ((mnFullans < scrollTop && scrollTop < mnContato)) {
          $scope.site.menu[3].active = true;
        } else if (mnContato < scrollTop) {
          $scope.site.menu[4].active = true;
        } else {
          $scope.site.menu[0].active = true;
        }
        return $scope.$digest();
      });
      isMobile = function() {
        if (w.width() < 992) {
          return true;
        }
      };
      return $scope.slideRight = function() {
        $scope.slideToRight = !$scope.slideToRight;
        if ($scope.slideToRight && isMobile()) {
          $scope.slideClass = "slide-right";
          return $scope.slideToLeft = false;
        } else {
          return $scope.slideClass = "";
        }
      };
    }
  ]).directive("floating", [
    function() {
      return {
        restrict: "C",
        link: function(scope, $element) {
          var $w, elem, elemBox, elemHeight, elemTop, isFixed;
          elem = $element;
          elemBox = $(".to-floating");
          elemTop = elem.offset().top;
          elemHeight = elem[0].offsetHeight;
          isFixed = false;
          $w = $(window);
          return $w.scroll(function() {
            var scrollTop, shouldBeFixed;
            elem.css({
              minHeight: elemHeight
            });
            scrollTop = $w.scrollTop();
            shouldBeFixed = scrollTop > elemTop;
            if (shouldBeFixed && !isFixed) {
              elem.addClass("active");
              elemBox.css({
                position: "fixed",
                top: 0,
                zIndex: 300,
                width: "100%"
              });
              return isFixed = true;
            } else if (!shouldBeFixed && isFixed) {
              elem.removeClass("active");
              elemBox.css({
                position: "relative"
              });
              return isFixed = false;
            }
          });
        },
        transclude: true,
        replace: true,
        template: "<div><div class=\"to-floating\" ng-transclude></div></div>"
      };
    }
  ]).directive("scrollTo", function() {
    return {
      restrict: "A",
      link: function(scope, $elm, attrs) {
        var idToScroll, marginTop;
        idToScroll = attrs.href;
        marginTop = 35;
        $elm.on("click", function(e) {
          var $target;
          e.preventDefault();
          $target = void 0;
          if (idToScroll) {
            $target = $(idToScroll);
          } else {
            $target = $elm;
          }
          $("body").animate({
            scrollTop: $target.offset().top - marginTop
          }, "slow");
        });
      }
    };
  });

}).call(this);
